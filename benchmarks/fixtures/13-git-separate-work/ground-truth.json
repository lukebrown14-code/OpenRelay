{
  "required": ["src/api.js"],
  "optional": [],
  "notes": "api.js must gain retry logic; the teammate WIP (config.yaml edit, wip-experiment.js, ideas.md) must survive untouched and uncommitted."
}
