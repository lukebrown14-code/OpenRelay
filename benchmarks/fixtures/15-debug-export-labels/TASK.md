# Task: CSV export shows a raw code

The orders table shows readable labels, but the CSV export shows raw “P2” for Priority. Fix the export so every priority has the same readable label as the table. Preserve status, region and CSV quoting. Do not edit verify.js. Run node verify.js.
